// Tailwind v4 is config-via-CSS — this file exists so other packages can
// `import "@web-tools/tailwind-preset/globals.css"` and inherit the theme.
// Tokens live in ./globals.css under @theme.
export const tailwindPreset = {
  name: "web-tools",
};
